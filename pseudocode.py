def sum_of_volumes(spec):
    # Compute the volume of each element, and sum them together.
    answer = 0
    return answer

  
def create_geometry(spec):
    # create geometry in CAD
    return

  
def compute_overall_bounding_box(spec):
    # Calculate the minimum bounding box that contains all objects
    answer = [0, 0, 0]
    return answer

  
def detect_intersection(spec):
    # return true if any shapes intersect, false if not.
    return False

